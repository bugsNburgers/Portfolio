// Shared constants used across the app

export const LOADER_DELAY = 2500; // ms total loader duration
export const NAV_SCROLL_THRESHOLD = 50; // px from top before nav shadow appears
export const NAV_HIDE_THRESHOLD = 100; // scroll delta to trigger hide/show
export const OTHER_PROJECTS_INITIAL_SHOW = 6;
export const ANIMATION_DURATION = 0.3;
export const STAGGER_DELAY = 0.1;
